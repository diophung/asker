# Runtime dependencies for services/clip (pinned; Python deps pinned per the
# M3 build contract). The HTTP layer uses the stdlib (http.server) — no web
# framework dependency. torch is the CPU build (see the --extra-index-url note
# below and the Dockerfile, which installs the CPU wheel explicitly).
#
# torch CPU wheels are published on the PyTorch index, not PyPI's default for
# every platform. The Dockerfile installs torch from the CPU index first; the
# version here pins what that resolves to.
open_clip_torch==2.24.0
torch==2.13.0
pillow==12.2.0
# Required by open_clip's image preprocessing (PIL image -> tensor). Without it
# image embedding fails at runtime with "Numpy is not available" while text
# embedding still works. Kept at numpy<2 — validated working with torch 2.12.0.
numpy==1.26.4

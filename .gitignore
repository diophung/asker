.DS_Store
/bin/
coverage.out
*.test
*.out
.env
.env.local
/web/node_modules/
/web/dist/
vespa/app.zip

# Compiled Go binaries built at repo root by `go build ./...`/`go run` (artifacts;
# the real binaries live in the service images). Covers every main package.
/control-plane
/gateway
/index-writer
/ingest
/query
/connector-hub
/enrich
/synthgen
/fake-gmail

# Python bytecode
__pycache__/
*.pyc

# Throwaway multi-agent workflow scripts (scratch; not part of the build)
.claude/wf-*.js
